[project]
name = "core-package"
version = "1.0.0"
description = "Core functionality package"
dependencies = ["requests>=2.28.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"