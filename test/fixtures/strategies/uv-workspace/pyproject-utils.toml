[project]
name = "utils-package"
version = "1.0.0"
description = "Utility functions package"
dependencies = []

[dependency-groups]
test = ["pytest>=7.0.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"